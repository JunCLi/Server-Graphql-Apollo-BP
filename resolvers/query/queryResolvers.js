const authenticate = require('../authenticate')

module.exports = {
  Query: {
		async placeholder(parent, {input}, {req, app, postgres}){
			try {

				return null
			} catch(err) {

			}
		},
  },
}
